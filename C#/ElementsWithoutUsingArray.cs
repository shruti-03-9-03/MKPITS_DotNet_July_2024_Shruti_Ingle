using System;

class Program
{
    static void Main()
    {
        // Initial element of the array is 2000
        int[] array = { 2000, 2001, 2002, 2003, 2004, 2005 };

        // Print the elements individually without using a loop
        Console.WriteLine(array[0]);
        Console.WriteLine(array[1]);
        Console.WriteLine(array[2]);
        Console.WriteLine(array[3]);
        Console.WriteLine(array[4]);
        Console.WriteLine(array[5]);
    }
}